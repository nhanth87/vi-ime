Đây là tài liệu redesign cho vi-ime để có thể gõ dc trên libeofice và onlyofice

lỗi hiện tại là khi gõ nhanh trên libre (VLC) và onllyofice (Chrome embedded) thì bị mất 1 vài chữ tiếng việt



Tóm tắt thay đổi kiến trúc

crates/vi-daemon/src/wayland/viet_[typer.rs](http://typer.rs): VietTyper giờ tự mở connection + EventQueue Wayland riêng (giống hệt EvdevTyper — pattern đã field-proven cho đường evdev), thay vì dùng chung connection với vòng lặp IME chính. Nhờ đó backspace_then_type có thể gọi roundtrip() thật (chờ compositor xác nhận đã xử lý) sau mỗi BackSpace, thay vì chỉ flush() như trước — đúng fix mà evdev_[typer.rs](http://typer.rs) đã áp dụng thành công từ 2026-07-10 nhưng chưa từng lan sang đường Wayland gốc.

crates/vi-daemon/src/wayland/[mod.rs](http://mod.rs): bỏ việc tạo viet_keyboard thứ hai trên connection chính (không cần nữa).

crates/vi-daemon/src/wayland/[state.rs](http://state.rs): ImeAppState::new() mất tham số viet_keyboard, gọi VietTyper::new() không tham số.

Đã ghi lại toàn bộ quyết định này vào [AGENTS.md](http://AGENTS.md) (R21) để không ai vô tình gộp lại connection trong tương lai.

Cần bạn test tay

1. Gõ lại chính xác "chữ tài chữ mệnh khéo là gét nhau" trong LibreOffice nhiều lần — đây là trường hợp đã lỗi.

2. Test thêm các từ có 2+ lần sửa dấu liên tiếp khác: "người", "được", "hưởng"...

3. Kiểm tra LibreOffice vẫn nhận đúng passthrough (Ctrl+C, mũi tên, phím tắt) — virtual_keyboard forwarder không đổi nhưng đáng kiểm lại vì đây là thay đổi kiến trúc.

4. Kiểm tra log lúc khởi động không có warning [VIET-TYPER] không mở được Wayland connection riêng (nếu có, nghĩa là compositor có vấn đề với việc mở 2 connection Wayland từ 1 process — báo lại ngay).

5. Test cả Chrome/terminal để chắc đường live-echo cho các app không dùng evdev fallback vẫn hoạt động bình thường (không chỉ riêng LibreOffice).

✻ Cogitated for 25m 30s

※ recap: Đang sửa lỗi mất chữ/mất dấu khi gõ trong LibreOffice và OnlyOffice. Đã fix xong 4 lỗi, code build và test pass; giờ cần bạn test tay lại các câu đã lỗi trước đó để xác nhận.

❯ libre, chữ ư bị thiếu dấu "nguời", onlyofice, lại bị lỗi nuốt chữ khi gõ nhanh, hãy fix và thêm regesiton test để cover hết hỗi

Trước khi vá tiếp, cần đọc log debug mới để xác nhận đúng root cause — đã sai 2 lần vì đoán trước khi đọc log, không lặp lại. Cũng cần kiểm tra scripts/vi-regression hiện có, vì đó là ngoại lệ hợp lệ của policy "không viết automation test" (harness field-level chạy tay).

  Read 2 files, ran 1 shell command

Đã hiểu cấu trúc regression harness — không có bài LibreOffice/OnlyOffice, cần thêm. Trước tiên đọc log debug để xác nhận đúng root cause của cả 2 lỗi mới.

  Searched for 2 patterns, read 1 file

Log này xác nhận đúng: đường Wayland gốc (VietTyper, đã đổi kiến trúc) gọi diff hoàn toàn đúng — bs=2 suffix="ươ" rồi bs=1 suffix="ờ" rồi bs=0 suffix="i" khớp chính xác "người". Không có [VIET-TYPER] không gõ được nào trong log. Nghĩa là backspace_then_type báo thành công ở tầng ứng dụng, nhưng bạn báo kết quả thật là "nguời" (thiếu dấu ư). Đây là dấu hiệu game y hệt bug cũ: VCL/gtk3 vẫn nuốt một phần của tap dù roundtrip đã xác nhận gửi xong — có thể vì SURROUNDING callback cho thấy độ trễ khá lớn (len tăng dần theo từng ký tự qua từng text_change_cause=Value(InputMethod), xác nhận LibreOffice CÓ nhận sự kiện, nhưng dường như chỉ nhận đúng phần ASCII cơ bản, không phải glyph có dấu).

Nhìn kỹ: SURROUNDING len=27 sau bước bs=2 suffix="ươ" (dòng 2317, 2319 — 2 dòng SURROUNDING liên tiếp cùng len=27, tức là 2 sự kiện text-change riêng biệt cho cùng độ dài). Đây gợi ý LibreOffice nhận backspace và ký tự tách rời thành 2 sự kiện, và có khả năng một trong hai ký tự "ư" hoặc "ơ" đã bị áp sai level. Cần xem đúng bytes.

  Searched for 1 pattern

Cần xem đoạn ONLYOFFICE (gõ nhanh) để chẩn đoán lỗi thứ hai trước khi vá cả hai cùng lúc.

  Searched for 4 patterns, read 2 files, ran 3 shell commands

Cần đọc kỹ đoạn ONLYOFFICE thứ hai (câu "chữ ư bị thiếu dấu... lại bị lỗi nuốt chữ khi gõ nhanh") để xác định từ nào thật sự bị lỗi và có đúng evidence log không.

  Searched for 2 patterns, read 1 file

● API Error: 402 Insufficient balance ($-2.40). Please top up.

✻ Brewed for 8m 6s