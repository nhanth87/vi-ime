# vi-im Debt & Trade-off Report — 2026-07-09

## 🔴 Không fix được (protocol/compositor limitation)

| # | Lỗi | Nguyên nhân | Impact |
|---|-----|-----------|--------|
| 1 | **Click chuột khi đang gõ → text nhảy theo** | Compositor xử lý mouse event trước IME commit. Deactivate đến sau khi cursor đã di chuyển. vi-im đã `engine.reset()` không commit, nhưng preedit có thể bị compositor convert sai vị trí. | Mất text đang gõ dở. Workaround: Space trước khi click. |
| 2 | **Gạch chân không thể tắt trong Preedit/NonPreedit** | zwp_input_method_v2 protocol — preedit luôn được app render với underline. Không có API để tắt. | Phiền với user muốn giao diện sạch. Terminal (foot/kitty) không hỗ trợ text_input_v3 nên không bị underline — nhưng đó là vì terminal ignore preedit. |
| 3 | **Settings preview không gõ được tiếng Việt** | Quickshell/QML không route IME commit → QML TextInput đúng cách. Qt layer shell không forward text_input_v3. | Đã xóa ô test khỏi settings UI. |
| 4 | **Address bar không gõ được tiếng Việt** | Trade-off: passthrough để Chrome autocomplete hoạt động (xem bảng dưới). Nếu muốn gõ tiếng Việt trên address bar → tắt IME hoặc dùng Google Search box. | Chỉ ảnh hưởng address bar. Mọi ô input khác bình thường. |

## 🟡 Trade-off đã đánh đổi

| # | Quyết định | Được | Mất |
|---|-----------|------|-----|
| 1 | **Address bar: passthrough** | Chrome autocomplete "chatgpt.com" hoạt động | Không gõ tiếng Việt trên address bar |
| 2 | **NonPreedit: hiện preedit khi has_pending** | Có visual feedback khi gõ | Có underline tạm thời |
| 3 | **Deactivate: drop thay vì commit** | Không corrupt text ở vị trí sai | Mất text nếu click chuột khi đang gõ |
| 4 | **Dùng input-method-v2 thay vì text-input-v3** | Hoạt động trên mọi app kể cả terminal | Không có IME popup, không surrounding-text cho NonPreedit |
| 5 | **Tray menu: 3 item riêng với ✓ check mark** | Hiển thị đúng method đang chọn | ksni cache menu, cần item riêng (3 thay vì 1 toggle) |
| 6 | **"Tự do" = Smart mode** | Gõ cả VNI lẫn Telex trong cùng 1 word | Tone mark digit conflict: '2' trong VNI là huyền, trong Telex là '2' literal → cần heuristic chọn |

## 🟢 Nợ kỹ thuật

| # | Mục | Mức độ | Ghi chú |
|---|-----|--------|---------|
| 1 | **Engine performance: 95ms average (8 samples)** | ⚠️ Cao | `stage_engine ema_us = 94725`. 8 slow samples từ 1248 keystrokes. Có thể do NFD normalization chậm hoặc godmod log allocation. Cần profile. |
| 2 | **Backspace chậm** | ⚠️ Trung bình | User báo backspace có delay. Chưa pinpoint được nguyên nhân. Có thể liên quan đến engine reparse mỗi backspace. |
| 3 | **Không detect được app (Chrome vs Terminal)** | ⏳ Thấp | `app_id` luôn là "?" trong telemetry. Compositor (Niri) không gửi app_id qua input-method-v2. Chỉ detect được qua ContentType field-level. |
| 4 | **Smart mode heuristic chưa hoàn chỉnh** | ⏳ Trung bình | Conflict VNI/Telex tone marks: digit '2' có thể là huyền (VNI) hoặc '2' literal (Telex). Cần thêm syllable dictionary để phân biệt. |
| 5 | **CommitWithBackspace: backspace_count không dùng** | ⏳ Thấp | Field `backspace_count` trong `CommitWithBackspace` được tính nhưng không dùng trong `apply_action`. Chỉ commit_string, không backspace raw keys đã forward. |
| 6 | **Không có hotkey toggle IME** | ⏳ Thấp | User phải vào tray menu để tắt/bật IME. Không có Ctrl+Shift hay phím tắt tương tự. |
| 7 | **2 warnings unreachable pattern đã fix** | ✅ Đã fix | Duplicate match arms trong ipc.rs và runtime.rs. |
| 8 | **R8 AGENTS.md đã cập nhật** | ✅ Đã fix | Deactivate: Drop, Don't Commit. |

## ✅ Đã fix trong session này

| # | Fix | Files |
|---|-----|-------|
| 1 | Tray menu: 3 item riêng với ✓ check mark, ksni refresh qua activate callback | `tray.rs` |
| 2 | Settings QML: +Tự do vào Kiểu gõ, -Hybrid khỏi Chế độ hiển thị, xóa ô test preview | `main.qml` |
| 3 | Deactivate: chỉ `engine.reset()`, không commit | `dispatch.rs` |
| 4 | ContentType::Url → passthrough address bar (autocomplete) | `dispatch.rs`, `actions.rs`, `state.rs` |
| 5 | InputMethod thêm Smart (Tự do), ImeMode chỉ còn Preedit/NonPreedit | `types.rs`, `engine.rs`, `config/types.rs` |
| 6 | NonPreedit: hiện preedit khi has_pending (visual feedback) | `actions.rs` |
| 7 | Serde alias "Tự do" → InputMethod::Smart | `config/types.rs` |
| 8 | R8 AGENTS.md: Deactivate Drop, Don't Commit | `AGENTS.md` |
| 9 | Duplicate match arms removed | `ipc.rs`, `runtime.rs` |
| 10 | NonPreedit xóa terminal special-case (preedit-everywhere) | `dispatch.rs`, `state.rs` |

## Chi tiết kiến trúc mới

```
InputMethod:   Telex | VNI | Smart (Tự do)
ImeMode:       Preedit | NonPreedit
FieldSensitivity: Normal | Secure | NumericRaw | Url

Pipeline:
  KeyEvent → ContentType gate (Secure/NumericRaw/Url → passthrough)
           → Engine push_key → reparse (NFD syllable process)
           → Action: Commit | UpdatePreedit | PassThrough
           → Apply: commit_string | set_preedit_string | vk.press
```
